import * as cdk from "@aws-cdk/core";
import { StackApp } from "./xxx__stackApp";
export declare class Ec2InstanceExampleStack extends StackApp {
    constructor(source: cdk.App, id: string, props?: cdk.StackProps);
}
